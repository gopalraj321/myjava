pipeline {
    agent any

    tools {
        maven 'Maven 3'
        jdk 'JDK 11'
    }

    environment {
        SONARQUBE = 'MySonar'
    }

    stages {
        stage('Checkout') {
            steps {
                git branch main  'https://github.com/gopalraj321/myjava.git'
            }
        }

        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                withSonarQubeEnv('MySonar') {
                    sh 'mvn sonar:sonar'
                }
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment step (optional)'
            }
        }
    }
}
